jdk:
  - openjdk25
before_install:
  - sdk install java 25.0.3-tem
  - sdk use java 25.0.3-tem
install:
  - chmod +x gradlew
  - ./gradlew :sourbycraft-api:build :sourbycraft-api:publishToMavenLocal -x test --no-daemon
