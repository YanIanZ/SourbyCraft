jdk:
  - openjdk25
before_install:
  - sdk install java 25.0.3-tem
  - sdk use java 25.0.3-tem
install:
  - chmod +x gradlew
  - ./gradlew :sourbyapi:build :sourbyapi:publishToMavenLocal -x test --no-daemon
