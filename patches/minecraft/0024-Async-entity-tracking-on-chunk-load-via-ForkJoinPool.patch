From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: SourbyCraft <dev@iyanz.dev>
Date: Tue, 13 May 2025 04:00:00 +0200
Subject: [PATCH] Async entity tracking on chunk load via ForkJoinPool


diff --git a/ca/spottedleaf/moonrise/patches/chunk_system/level/entity/EntityLookup.java b/ca/spottedleaf/moonrise/patches/chunk_system/level/entity/EntityLookup.java
index 2d24d03bbdb5ee0d862cbfff2219f58afffafe12..2f7d74bd1ce40f4e41ef7b8ec30b79342165bcde 100644
--- a/ca/spottedleaf/moonrise/patches/chunk_system/level/entity/EntityLookup.java
+++ b/ca/spottedleaf/moonrise/patches/chunk_system/level/entity/EntityLookup.java
@@ -52,6 +52,8 @@ public abstract class EntityLookup implements LevelEntityGetter<Entity> {
     protected final ConcurrentHashMap<UUID, Entity> entityByUUID = new ConcurrentHashMap<>();
     protected final EntityList accessibleEntities = new EntityList();
 
+    public volatile boolean bypassThreadCheck = false; // SourbyCraft - async chunk load
+
     public EntityLookup(final Level world, final LevelCallback<Entity> worldCallback) {
         this.world = world;
         this.worldCallback = worldCallback;
@@ -233,7 +235,7 @@ public abstract class EntityLookup implements LevelEntityGetter<Entity> {
 
     public void entityStatusChange(final Entity entity, final ChunkEntitySlices slices, final Visibility oldVisibility, final Visibility newVisibility, final boolean moved,
                                    final boolean created, final boolean destroyed) {
-        this.checkThread(entity, "Entity status change must only happen on the main thread");
+        if (!this.bypassThreadCheck) this.checkThread(entity, "Entity status change must only happen on the main thread"); // SourbyCraft - async chunk load
 
         if (((ChunkSystemEntity)entity).moonrise$isUpdatingSectionStatus()) {
             // recursive status update
@@ -406,7 +408,7 @@ public abstract class EntityLookup implements LevelEntityGetter<Entity> {
         final int sectionX = pos.getX() >> 4;
         final int sectionY = Mth.clamp(pos.getY() >> 4, WorldUtil.getMinSection(this.world), WorldUtil.getMaxSection(this.world));
         final int sectionZ = pos.getZ() >> 4;
-        this.checkThread(sectionX, sectionZ, "Cannot add entity off-main thread");
+        if (!this.bypassThreadCheck) this.checkThread(sectionX, sectionZ, "Cannot add entity off-main thread"); // SourbyCraft - async chunk load
 
         if (entity.isRemoved()) {
             LOGGER.warn("Refusing to add removed entity: " + entity);
diff --git a/net/minecraft/server/level/ServerLevel.java b/net/minecraft/server/level/ServerLevel.java
index ad128afbd6f3b1a3dec43866261b35b6232d1f76..1166810f08ec8c5e5252335b1685e8f43d19559e 100644
--- a/net/minecraft/server/level/ServerLevel.java
+++ b/net/minecraft/server/level/ServerLevel.java
@@ -27,6 +27,7 @@ import java.util.Optional;
 import java.util.Set;
 import java.util.UUID;
 import java.util.concurrent.Executor;
+import java.util.concurrent.ForkJoinPool;
 import java.util.function.BooleanSupplier;
 import java.util.function.Function;
 import java.util.function.Predicate;
@@ -2565,7 +2566,13 @@ public class ServerLevel extends Level implements ServerEntityGetter, WorldGenLe
     }
     public void addLegacyChunkEntities(Stream<Entity> entities, ChunkPos chunkPos) {
         // Paper end - add chunkpos param
-        this.moonrise$getEntityLookup().addLegacyChunkEntities(entities.toList(), chunkPos); // Paper - rewrite chunk system
+        // SourbyCraft start - async chunk load
+        if (dev.iyanz.sourbycraft.SourbyCraftConfig.asyncChunkLoad) {
+            this.processChunkEntitiesAsync(entities.toList(), chunkPos, true);
+        } else {
+            this.moonrise$getEntityLookup().addLegacyChunkEntities(entities.toList(), chunkPos); // Paper - rewrite chunk system
+        }
+        // SourbyCraft end - async chunk load
     }
 
     public void addWorldGenChunkEntities(Stream<Entity> entities) {
@@ -2574,8 +2581,32 @@ public class ServerLevel extends Level implements ServerEntityGetter, WorldGenLe
     }
     public void addWorldGenChunkEntities(Stream<Entity> entities, ChunkPos chunkPos) {
         // Paper end - add chunkpos param
-        this.moonrise$getEntityLookup().addWorldGenChunkEntities(entities.toList(), chunkPos); // Paper - rewrite chunk system
+        // SourbyCraft start - async chunk load
+        if (dev.iyanz.sourbycraft.SourbyCraftConfig.asyncChunkLoad) {
+            this.processChunkEntitiesAsync(entities.toList(), chunkPos, false);
+        } else {
+            this.moonrise$getEntityLookup().addWorldGenChunkEntities(entities.toList(), chunkPos); // Paper - rewrite chunk system
+        }
+        // SourbyCraft end - async chunk load
+    }
+
+    // SourbyCraft start - async chunk load
+    private void processChunkEntitiesAsync(java.util.List<Entity> entities, ChunkPos chunkPos, boolean fromDisk) {
+        final ca.spottedleaf.moonrise.patches.chunk_system.level.entity.EntityLookup entityLookup = this.moonrise$getEntityLookup();
+        ForkJoinPool.commonPool().execute(() -> {
+            entityLookup.bypassThreadCheck = true;
+            try {
+                if (fromDisk) {
+                    entityLookup.addLegacyChunkEntities(entities, chunkPos);
+                } else {
+                    entityLookup.addWorldGenChunkEntities(entities, chunkPos);
+                }
+            } finally {
+                entityLookup.bypassThreadCheck = false;
+            }
+        });
     }
+    // SourbyCraft end - async chunk load
 
     public void startTickingChunk(LevelChunk chunk) {
         chunk.unpackTicks(this.getGameTime());
