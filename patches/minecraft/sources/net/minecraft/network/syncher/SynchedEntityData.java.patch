--- a/net/minecraft/network/syncher/SynchedEntityData.java
+++ b/net/minecraft/network/syncher/SynchedEntityData.java
@@ -21,6 +_,28 @@
     private final SynchedEntityData.DataItem<?>[] itemsById;
     private boolean isDirty;
 
+    private static final ThreadLocal<java.util.ArrayDeque<DataValue<?>>> VALUE_POOL =
+        ThreadLocal.withInitial(java.util.ArrayDeque::new);
+
+    @SuppressWarnings("unchecked")
+    static <T> DataValue<T> borrowValue(int id, EntityDataSerializer<T> serializer, T value) {
+        if (!dev.iyanz.sourbycraft.SourbyCraftConfig.poolEntityData) {
+            return new DataValue<>(id, serializer, value);
+        }
+        DataValue<?> dv = VALUE_POOL.get().pollLast();
+        if (dv != null) {
+            ((DataValue) dv).set(id, serializer, value);
+            return (DataValue<T>) dv;
+        }
+        return new DataValue<>(id, serializer, value);
+    }
+
+    static void returnValue(DataValue<?> value) {
+        if (dev.iyanz.sourbycraft.SourbyCraftConfig.poolEntityData && value != null) {
+            VALUE_POOL.get().addLast(value);
+        }
+    }
+
     SynchedEntityData(SyncedDataHolder entity, SynchedEntityData.DataItem<?>[] itemsById) {
         this.entity = entity;
         this.itemsById = itemsById;
@@ -114,9 +_,10 @@
 
     public void assignValues(List<SynchedEntityData.DataValue<?>> entries) {
         for (SynchedEntityData.DataValue<?> dataValue : entries) {
-            SynchedEntityData.DataItem<?> dataItem = this.itemsById[dataValue.id];
+            SynchedEntityData.DataItem<?> dataItem = this.itemsById[dataValue.id()];
             this.assignValue(dataItem, dataValue);
             this.entity.onSyncedDataUpdated(dataItem.getAccessor());
+            returnValue(dataValue);
         }
 
         this.entity.onSyncedDataUpdated(entries);
@@ -229,10 +_,30 @@
         }
     }
 
-    public record DataValue<T>(int id, EntityDataSerializer<T> serializer, T value) {
+    public static final class DataValue<T> {
+        private int id;
+        private EntityDataSerializer<T> serializer;
+        private T value;
+
+        public DataValue(int id, EntityDataSerializer<T> serializer, T value) {
+            this.id = id;
+            this.serializer = serializer;
+            this.value = value;
+        }
+
+        public int id() { return this.id; }
+        public EntityDataSerializer<T> serializer() { return this.serializer; }
+        public T value() { return this.value; }
+
+        void set(int id, EntityDataSerializer<T> serializer, T value) {
+            this.id = id;
+            this.serializer = serializer;
+            this.value = value;
+        }
+
         public static <T> SynchedEntityData.DataValue<T> create(EntityDataAccessor<T> dataAccessor, T value) {
             EntityDataSerializer<T> entityDataSerializer = dataAccessor.serializer();
-            return new SynchedEntityData.DataValue<>(dataAccessor.id(), entityDataSerializer, entityDataSerializer.copy(value));
+            return borrowValue(dataAccessor.id(), entityDataSerializer, entityDataSerializer.copy(value));
         }
 
         public void write(RegistryFriendlyByteBuf buffer) {
@@ -257,7 +_,7 @@
         }
 
         private static <T> SynchedEntityData.DataValue<T> read(RegistryFriendlyByteBuf buffer, int id, EntityDataSerializer<T> serializer) {
-            return new SynchedEntityData.DataValue<>(id, serializer, serializer.codec().decode(buffer));
+            return borrowValue(id, serializer, serializer.codec().decode(buffer));
         }
     }
 }
