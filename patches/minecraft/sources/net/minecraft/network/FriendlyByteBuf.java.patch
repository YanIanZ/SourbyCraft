--- a/net/minecraft/network/FriendlyByteBuf.java
+++ b/net/minecraft/network/FriendlyByteBuf.java
@@ -87,6 +_,15 @@
         this.source = source;
     }
 
+    // SourbyCraft start - pre-size chunk packet buffers
+    public static FriendlyByteBuf createChunkBuffer() {
+        if (dev.iyanz.sourbycraft.SourbyCraftConfig.preSizePackets) {
+            return new FriendlyByteBuf(io.netty.buffer.Unpooled.buffer(8192));
+        }
+        return new FriendlyByteBuf(io.netty.buffer.Unpooled.buffer());
+    }
+    // SourbyCraft end - pre-size chunk packet buffers
+
     @Deprecated
     public <T> T readWithCodecTrusted(DynamicOps<Tag> ops, Codec<T> codec) {
         return this.readWithCodec(ops, codec, NbtAccounter.unlimitedHeap());
