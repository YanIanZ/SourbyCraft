--- a/net/minecraft/network/protocol/game/ClientboundLevelChunkPacketData.java
+++ b/net/minecraft/network/protocol/game/ClientboundLevelChunkPacketData.java
@@ -57,7 +_,12 @@
         if (chunkPacketInfo != null) {
             chunkPacketInfo.setBuffer(this.buffer);
         }
-        extractChunkData(new FriendlyByteBuf(this.getWriteBuffer()), levelChunk, chunkPacketInfo);
+        // SourbyCraft start - pre-size chunk packet buffers
+        FriendlyByteBuf writeBuf = FriendlyByteBuf.createChunkBuffer();
+        extractChunkData(writeBuf, levelChunk, chunkPacketInfo, this.buffer.length);
+        writeBuf.readBytes(this.buffer);
+        writeBuf.release();
+        // SourbyCraft end - pre-size chunk packet buffers
         this.blockEntitiesData = Lists.newArrayList();
         int totalTileEntities = 0; // Paper - Handle oversized block entities in chunks
 
@@ -75,6 +_,40 @@
         }
     }
 
+    // SourbyCraft start - chunk compression cache
+    public static ClientboundLevelChunkPacketData createFromCache(LevelChunk levelChunk, byte[] preSerializedBuffer) {
+        return new ClientboundLevelChunkPacketData(levelChunk, preSerializedBuffer);
+    }
+
+    private ClientboundLevelChunkPacketData(LevelChunk levelChunk, byte[] preSerializedBuffer) {
+        this.heightmaps = new long[HEIGHTMAP_TYPES_LEN][];
+        int heightmapsCount = 0;
+        for (int i = 0; i < SENDABLE_HEIGHTMAP_TYPES_LEN; ++i) {
+            int typeId = SENDABLE_HEIGHTMAP_TYPE_IDS[i];
+            Heightmap heightmap = levelChunk.heightmaps[typeId];
+            if (heightmap != null) {
+                this.heightmaps[typeId] = heightmap.getRawData();
+                heightmapsCount++;
+            }
+        }
+        this.heightmapsCount = heightmapsCount;
+        this.buffer = preSerializedBuffer;
+        this.blockEntitiesData = Lists.newArrayList();
+        int totalTileEntities = 0;
+
+        for (java.util.Map.Entry<BlockPos, BlockEntity> entry : levelChunk.getBlockEntities().entrySet()) {
+            if (++totalTileEntities > BLOCK_ENTITY_LIMIT) {
+                net.minecraft.network.protocol.Packet<ClientGamePacketListener> packet = entry.getValue().getUpdatePacket();
+                if (packet != null) {
+                    this.extraPackets.add(packet);
+                    continue;
+                }
+            }
+            this.blockEntitiesData.add(BlockEntityInfo.create(entry.getValue()));
+        }
+    }
+    // SourbyCraft end - chunk compression cache
+
     public ClientboundLevelChunkPacketData(RegistryFriendlyByteBuf buffer, int x, int z) {
         this.heightmaps = HEIGHTMAPS_STREAM_CODEC.decode(buffer);
         int varInt = buffer.readVarInt();
@@ -94,7 +_,7 @@
         ClientboundLevelChunkPacketData.BlockEntityInfo.LIST_STREAM_CODEC.encode(buffer, this.blockEntitiesData);
     }
 
-    private static int calculateChunkSize(LevelChunk chunk) {
+    public static int calculateChunkSize(LevelChunk chunk) {
         int i = 0;
 
         for (LevelChunkSection levelChunkSection : chunk.getSections()) {
@@ -113,9 +_,14 @@
     // Paper start - Anti-Xray - Add chunk packet info
     @Deprecated @io.papermc.paper.annotation.DoNotUse
     public static void extractChunkData(FriendlyByteBuf buffer, LevelChunk chunk) {
-        ClientboundLevelChunkPacketData.extractChunkData(buffer, chunk, null);
+        ClientboundLevelChunkPacketData.extractChunkData(buffer, chunk, null, 0);
     }
     public static void extractChunkData(FriendlyByteBuf buffer, LevelChunk chunk, io.papermc.paper.antixray.ChunkPacketInfo<net.minecraft.world.level.block.state.BlockState> chunkPacketInfo) {
+        ClientboundLevelChunkPacketData.extractChunkData(buffer, chunk, chunkPacketInfo, 0);
+    }
+    // SourbyCraft start - pre-size chunk packet buffers
+    public static void extractChunkData(FriendlyByteBuf buffer, LevelChunk chunk, io.papermc.paper.antixray.ChunkPacketInfo<net.minecraft.world.level.block.state.BlockState> chunkPacketInfo, int expectedSize) {
+        // SourbyCraft end - pre-size chunk packet buffers
         int chunkSectionIndex = 0;
         for (LevelChunkSection levelChunkSection : chunk.getSections()) {
             levelChunkSection.write(buffer, chunkPacketInfo, chunkSectionIndex);
@@ -123,9 +_,12 @@
             // Paper end  - Anti-Xray - Add chunk packet info
         }
 
-        if (buffer.writerIndex() != buffer.capacity()) {
-            throw new IllegalStateException("Didn't fill chunk buffer: expected " + buffer.capacity() + " bytes, got " + buffer.writerIndex());
+        // SourbyCraft start - pre-size chunk packet buffers - check against expected size
+        int checkSize = expectedSize > 0 ? expectedSize : buffer.capacity();
+        if (buffer.writerIndex() != checkSize) {
+            throw new IllegalStateException("Didn't fill chunk buffer: expected " + checkSize + " bytes, got " + buffer.writerIndex());
         }
+        // SourbyCraft end - pre-size chunk packet buffers
     }
 
     public Consumer<ClientboundLevelChunkPacketData.BlockEntityTagOutput> getBlockEntitiesTagsConsumer(int chunkX, int chunkZ) {
