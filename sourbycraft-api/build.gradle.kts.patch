--- a/canvas-api/build.gradle.kts
+++ b/canvas-api/build.gradle.kts
@@ -4,7 +_,10 @@
     `java-library`
     `maven-publish`
     idea
-    id("io.canvasmc.weaver.dependency-bridge")
+    // SourbyCraft-on-Canvas - io.canvasmc.weaver.dependency-bridge has no sourbypatcher
+    // equivalent (sourbypatcher only ships "core"/"patcher", mirroring upstream
+    // paperweight-core). It is a Canvas-internal convention plugin for
+    // server/api dependency-version syncing; dropped here, not required to compile.
 }
 
 java {
@@ -107,17 +_,21 @@
         java {
             srcDir(generatedDir)
             srcDir(file("../paper-api/src/main/java"))
+            srcDir(file("../canvas-api/src/main/java"))
         }
         resources {
             srcDir(file("../paper-api/src/main/resources"))
+            srcDir(file("../canvas-api/src/main/resources"))
         }
     }
     test {
         java {
             srcDir(file("../paper-api/src/test/java"))
+            srcDir(file("../canvas-api/src/test/java"))
         }
         resources {
             srcDir(file("../paper-api/src/test/resources"))
+            srcDir(file("../canvas-api/src/test/resources"))
         }
     }
 }
