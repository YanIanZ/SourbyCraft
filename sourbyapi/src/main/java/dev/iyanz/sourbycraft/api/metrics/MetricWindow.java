package dev.iyanz.sourbycraft.api.metrics;

/** Fixed lookback windows available in a performance snapshot. */
public enum MetricWindow {
    FIVE_SECONDS,
    TEN_SECONDS,
    ONE_MINUTE,
    FIVE_MINUTES,
    FIFTEEN_MINUTES
}
