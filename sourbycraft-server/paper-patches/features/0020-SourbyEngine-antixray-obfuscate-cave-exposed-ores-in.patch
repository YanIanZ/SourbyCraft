From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Yan <Yan>
Date: Tue, 14 Jul 2026 20:05:15 +0700
Subject: [PATCH] SourbyEngine antixray: obfuscate cave-exposed ores in packet
 (packet-level x-ray defense)


diff --git a/src/main/java/io/papermc/paper/antixray/ChunkPacketBlockControllerAntiXray.java b/src/main/java/io/papermc/paper/antixray/ChunkPacketBlockControllerAntiXray.java
index 829fd9be43d95928cf6c8df364aa48d469dad142..903fe8778e9f72b037dec606b3825f7ee734ccf9 100644
--- a/src/main/java/io/papermc/paper/antixray/ChunkPacketBlockControllerAntiXray.java
+++ b/src/main/java/io/papermc/paper/antixray/ChunkPacketBlockControllerAntiXray.java
@@ -57,6 +57,13 @@ public final class ChunkPacketBlockControllerAntiXray extends ChunkPacketBlockCo
     private final int[] presetBlockStateBitsEndStoneGlobal;
     private final boolean[] solidGlobal = new boolean[Block.BLOCK_STATE_REGISTRY.size()];
     private final boolean[] obfuscateGlobal = new boolean[Block.BLOCK_STATE_REGISTRY.size()];
+    // SourbyEngine (packet-level anti-xray): when true, a configured hidden block (ore) is obfuscated
+    // to the preset (stone/deepslate/…) in the chunk packet even when it is cave-EXPOSED, not just when
+    // fully occluded. The real ore state therefore NEVER leaves the server, so no client-side x-ray can
+    // read it — SourbyCraft's raytrace layer then reveals only the ores a player genuinely has line of
+    // sight to (per-player block updates). Set from antixray.hide-exposed-ores (default true); flip
+    // false to fall back to vanilla Paper occlusion-only hiding.
+    public static volatile boolean HIDE_EXPOSED_ORES = true;
     private final LevelChunkSection[] emptyNearbyChunkSections = {EMPTY_SECTION, EMPTY_SECTION, EMPTY_SECTION, EMPTY_SECTION};
     private final int maxBlockHeightUpdatePosition;
 
@@ -386,6 +393,10 @@ public final class ChunkPacketBlockControllerAntiXray extends ChunkPacketBlockCo
     }
 
     private void obfuscateLayer(int y, BitStorageReader bitStorageReader, BitStorageWriter bitStorageWriter, boolean[] solid, boolean[] obfuscate, int[] presetBlockStateBits, boolean[][] current, boolean[][] next, boolean[][] nextNext, LevelChunkSection[] nearbyChunkSections, IntSupplier random) {
+        // SourbyEngine: when set, an obfuscate[] block (ore) is written as preset REGARDLESS of exposure
+        // (the `!obfuscate[bits] &&` guard below drops the keep-real path for it) — so exposed cave ores
+        // are hidden in the packet too. Read once per layer; non-ore blocks keep vanilla occlusion logic.
+        final boolean hideExposed = HIDE_EXPOSED_ORES;
         // First block of first line
         int bits = bitStorageReader.read();
 
@@ -394,7 +405,7 @@ public final class ChunkPacketBlockControllerAntiXray extends ChunkPacketBlockCo
             next[0][1] = true;
             next[1][0] = true;
         } else {
-            if (current[0][0] || isTransparent(nearbyChunkSections[2], 0, y, 15) || isTransparent(nearbyChunkSections[0], 15, y, 0)) {
+            if ((!hideExposed || !obfuscate[bits]) && (current[0][0] || isTransparent(nearbyChunkSections[2], 0, y, 15) || isTransparent(nearbyChunkSections[0], 15, y, 0))) {
                 bitStorageWriter.skip();
             } else {
                 bitStorageWriter.write(presetBlockStateBits[random.getAsInt()]);
@@ -415,7 +426,7 @@ public final class ChunkPacketBlockControllerAntiXray extends ChunkPacketBlockCo
                 next[0][x + 1] = true;
                 next[1][x] = true;
             } else {
-                if (current[0][x] || isTransparent(nearbyChunkSections[2], x, y, 15)) {
+                if ((!hideExposed || !obfuscate[bits]) && (current[0][x] || isTransparent(nearbyChunkSections[2], x, y, 15))) {
                     bitStorageWriter.skip();
                 } else {
                     bitStorageWriter.write(presetBlockStateBits[random.getAsInt()]);
@@ -435,7 +446,7 @@ public final class ChunkPacketBlockControllerAntiXray extends ChunkPacketBlockCo
             next[0][14] = true;
             next[1][15] = true;
         } else {
-            if (current[0][15] || isTransparent(nearbyChunkSections[2], 15, y, 15) || isTransparent(nearbyChunkSections[1], 0, y, 0)) {
+            if ((!hideExposed || !obfuscate[bits]) && (current[0][15] || isTransparent(nearbyChunkSections[2], 15, y, 15) || isTransparent(nearbyChunkSections[1], 0, y, 0))) {
                 bitStorageWriter.skip();
             } else {
                 bitStorageWriter.write(presetBlockStateBits[random.getAsInt()]);
@@ -457,7 +468,7 @@ public final class ChunkPacketBlockControllerAntiXray extends ChunkPacketBlockCo
                 next[z - 1][0] = true;
                 next[z + 1][0] = true;
             } else {
-                if (current[z][0] || isTransparent(nearbyChunkSections[0], 15, y, z)) {
+                if ((!hideExposed || !obfuscate[bits]) && (current[z][0] || isTransparent(nearbyChunkSections[0], 15, y, z))) {
                     bitStorageWriter.skip();
                 } else {
                     bitStorageWriter.write(presetBlockStateBits[random.getAsInt()]);
@@ -479,7 +490,7 @@ public final class ChunkPacketBlockControllerAntiXray extends ChunkPacketBlockCo
                     next[z - 1][x] = true;
                     next[z + 1][x] = true;
                 } else {
-                    if (current[z][x]) {
+                    if ((!hideExposed || !obfuscate[bits]) && current[z][x]) {
                         bitStorageWriter.skip();
                     } else {
                         bitStorageWriter.write(presetBlockStateBits[random.getAsInt()]);
@@ -500,7 +511,7 @@ public final class ChunkPacketBlockControllerAntiXray extends ChunkPacketBlockCo
                 next[z - 1][15] = true;
                 next[z + 1][15] = true;
             } else {
-                if (current[z][15] || isTransparent(nearbyChunkSections[1], 0, y, z)) {
+                if ((!hideExposed || !obfuscate[bits]) && (current[z][15] || isTransparent(nearbyChunkSections[1], 0, y, z))) {
                     bitStorageWriter.skip();
                 } else {
                     bitStorageWriter.write(presetBlockStateBits[random.getAsInt()]);
@@ -520,7 +531,7 @@ public final class ChunkPacketBlockControllerAntiXray extends ChunkPacketBlockCo
             next[15][1] = true;
             next[14][0] = true;
         } else {
-            if (current[15][0] || isTransparent(nearbyChunkSections[3], 0, y, 0) || isTransparent(nearbyChunkSections[0], 15, y, 15)) {
+            if ((!hideExposed || !obfuscate[bits]) && (current[15][0] || isTransparent(nearbyChunkSections[3], 0, y, 0) || isTransparent(nearbyChunkSections[0], 15, y, 15))) {
                 bitStorageWriter.skip();
             } else {
                 bitStorageWriter.write(presetBlockStateBits[random.getAsInt()]);
@@ -541,7 +552,7 @@ public final class ChunkPacketBlockControllerAntiXray extends ChunkPacketBlockCo
                 next[15][x + 1] = true;
                 next[14][x] = true;
             } else {
-                if (current[15][x] || isTransparent(nearbyChunkSections[3], x, y, 0)) {
+                if ((!hideExposed || !obfuscate[bits]) && (current[15][x] || isTransparent(nearbyChunkSections[3], x, y, 0))) {
                     bitStorageWriter.skip();
                 } else {
                     bitStorageWriter.write(presetBlockStateBits[random.getAsInt()]);
@@ -561,7 +572,7 @@ public final class ChunkPacketBlockControllerAntiXray extends ChunkPacketBlockCo
             next[15][14] = true;
             next[14][15] = true;
         } else {
-            if (current[15][15] || isTransparent(nearbyChunkSections[3], 15, y, 0) || isTransparent(nearbyChunkSections[1], 0, y, 15)) {
+            if ((!hideExposed || !obfuscate[bits]) && (current[15][15] || isTransparent(nearbyChunkSections[3], 15, y, 0) || isTransparent(nearbyChunkSections[1], 0, y, 15))) {
                 bitStorageWriter.skip();
             } else {
                 bitStorageWriter.write(presetBlockStateBits[random.getAsInt()]);
